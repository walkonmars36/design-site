---
---

<section class="wrapper">
  <div id="contact">
    <h2>Get in touch</h2>
    <div class="contact__cta flex column">
      <img src="/assets/images/mark.png" alt="mark" width="100" height="100" />
      <p class="large">
        Lorem ipsum dolor sit amet consectetur adipisicing elit. Unde
        consequuntur blanditiis hic. Dicta, voluptatum cupiditate! Enim animi
        distinctio officiis non? Nobis earum odit numquam deserunt sunt facere,
        possimus quia officia?
      </p>
      <button class="btn self-center contact__go">Let's Go</button>
    </div>
  </div>

  <p class="large white">
    Or, if you’d like a quick face to face chat, make an appointment below.
  </p>
  <div class="calendar">
    <!-- Cal inline embed code begins -->
    <div style="width:100%;height:100%;overflow:scroll" id="my-cal-inline">
    </div>
    <script type="text/javascript">
      (function (C, A, L) {
        let p = function (a, ar) {
          a.q.push(ar);
        };
        let d = C.document;
        C.Cal =
          C.Cal ||
          function () {
            let cal = C.Cal;
            let ar = arguments;
            if (!cal.loaded) {
              cal.ns = {};
              cal.q = cal.q || [];
              d.head.appendChild(d.createElement("script")).src = A;
              cal.loaded = true;
            }
            if (ar[0] === L) {
              const api = function () {
                p(api, arguments);
              };
              const namespace = ar[1];
              api.q = api.q || [];
              typeof namespace === "string"
                ? (cal.ns[namespace] = api) && p(api, ar)
                : p(cal, ar);
              return;
            }
            p(cal, ar);
          };
      })(window, "https://app.cal.com/embed/embed.js", "init");
      Cal("init", { origin: "https://cal.com" });

      Cal("inline", {
        elementOrSelector: "#my-cal-inline",
        calLink: "walkonmars.design/15min",
        layout: "month_view",
      });

      Cal("ui", {
        styles: { branding: { brandColor: "#1d1e20" } },
        hideEventTypeDetails: false,
        layout: "month_view",
      });
    </script>
    <!-- Cal inline embed code ends -->
  </div>
</section>

<style>
  section {
    padding-block: var(--space-2xl-3xl);
  }

  .contact__cta {
    background: var(--color-accent-400);
    border-radius: var(--border-radius-xl);
    padding: var(--space-l);
    margin-top: var(--space-l);
  }

  .large {
    color: var(--color-secondary-400);
    font-weight: 700;
    line-height: 1.2;
    padding: var(--space-s) var(--space-l);
  }

  .large.white {
    color: var(--color-white);
    max-width: 35ch;
    padding: 0;
    margin-top: var(--space-3xl);
  }

  button.contact__go {
    font-weight: 700;
    color: var(--color-primary);
    background: var(--color-secondary-500);
    padding: 0.5em 1.75em;
    border-radius: var(--border-radius-sm);
  }

  .calendar {
    margin-top: var(--space-3xl);
    background: var(--color-secondary-300);
  }
</style>
