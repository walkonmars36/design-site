---

---

<button
  class="nav-toggle"
  aria-controls="primary-navigation"
  aria-label="Open Menu"
  aria-expanded="false"
>
  <svg
    fill="var(--hamburger-color)"
    class="hamburger"
    viewBox="0 0 100 100"
    width="60"
  >
    <rect class="line top" width="60" height="8" x="20" y="27" rx="5"> </rect>
    <rect class="line middle" width="60" height="8" x="20" y="45" rx="5">
    </rect>
    <rect class="line bottom" width="60" height="8" x="20" y="63" rx="5">
    </rect>
  </svg>
</button>

<style>
  button {
    background: var(--color-accent-400);
    border: none;
    display: grid;
    border-radius: 50%;
  }

  .nav-toggle {
    z-index: 100;
  }

  .nav-toggle .line {
    transition:
      transform 300ms ease-in,
      opacity 0ms 300ms;
    transform-origin: center;
  }

  .nav-toggle[aria-expanded="true"] .line {
    transition:
      transform 300ms ease-in,
      opacity 0ms 300ms;
  }

  .nav-toggle[aria-expanded="true"] .middle {
    transition:
      transform 300ms ease-out,
      opacity 300ms ease-out;
    transform: translateX(100%);
    opacity: 0;
  }

  .nav-toggle[aria-expanded="true"] .top {
    transform: translateY(10%) rotate(45deg);
  }

  .nav-toggle[aria-expanded="true"] .bottom {
    transform: translateY(-15%) rotate(-45deg);
  }

  @media (min-width: 992px) {
    .nav-toggle {
      display: none;
    }
  }
</style>
